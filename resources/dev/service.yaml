apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  selector:
    app: password-generator
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 80
